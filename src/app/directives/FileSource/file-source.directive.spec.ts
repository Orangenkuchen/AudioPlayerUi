import { FileSourceDirective } from './file-source.directive';

describe('FileSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSourceDirective();
    expect(directive).toBeTruthy();
  });
});
